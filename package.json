{
  "name": "built-with-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:flappy-bird && npm run build:snake-on-windows-xp",
    "build:flappy-bird": "cd apps/flappy-bird && npm install && npm run build",
    "build:snake-on-windows-xp": "cd apps/snake-on-windows-xp && npm install && npm run build"
  }
}
