{
	"name": "built-with-ai",
	"private": true,
	"version": "1.0.0",
	"scripts": {
		"build": "yarn build:flappy-bird && yarn build:snake-on-windows-xp",
		"build:flappy-bird": "cd apps/flappy-bird && yarn && yarn build",
		"build:snake-on-windows-xp": "cd apps/snake-on-windows-xp && yarn && yarn build"
	}
}
